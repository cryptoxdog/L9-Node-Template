# Governance

Governance is template-level enforcement, not runtime node behavior. The governance layer protects contracts, generated-file boundaries, transport rules, CI gates, and drift-sensitive files.

Validation gates include contract verification, audit engine checks, template compliance, generated-boundary validation, strict pyright configuration, and coverage threshold enforcement.
