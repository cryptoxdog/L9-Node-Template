# Unknown Register

- Whether `l9_sdk.transport.TransportPacket` is installable in the target environment.
- Whether remote CI passes after dependencies are resolved.
- Whether concrete nodes will use this template through a CLI, GitHub template, or package manager.
- Exact schema of production `nodespec.yaml` beyond the reference input contract.
